import { useState, useEffect } from 'react';
import { w3cwebsocket as W3WebSocket } from 'websocket';

export const WebSocket = () => {
  const [address, setAddress] = useState('ws://localhost:42069');

  useEffect()
}